define host {
  host_name		{{ inventory_hostname }}
{% for option in [ 
	'alias',
	'address',
	'max_check_attempts',
	'check_period',
	'contacts',
	'contact_groups',
	'notification_interval',
	'notification_period',
	'use',
	'display_name',
	'parents',
	'hourly_value',
	'name',
	'register',
	'check_command',
	'hostgroups' ] %}
{% if option in item.keys() %}
  {{ option }}		{{ item[option] }}
{% endif %}
{% endfor %}
}
